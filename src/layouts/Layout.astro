<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="generator" content={Astro.generator} />
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22></text></svg>">
		<title>Hermes | Fotografía</title>

    <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollToPlugin.min.js"></script>

	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
:root {
    --color-hermes: #ba1d3d; /* CAMBIA EL COLOR AQUÍ (ej: blanco) */
}
  /* --- Estilos para Chrome, Safari y Edge --- */
::-webkit-scrollbar {
  width: 6px; /* Muy fino y minimalista */
  height: 6px; /* Para scroll horizontal */
}

/* El fondo del carril del scroll */
::-webkit-scrollbar-track {
  background: #101828; /* El azul profundo de tu fondo */
}

/* La barra que se mueve (el "thumb") */
::-webkit-scrollbar-thumb {
  background: #2d3436; /* Un gris oscuro sutil para que no distraiga */
  border-radius: 10px;
}

/* Cuando el usuario pasa el mouse por encima de la barra */
::-webkit-scrollbar-thumb:hover {
  background: #EA0E3A; /* Tu rojo vibrante: un toque de color solo al interactuar */
}

/* --- Estilos para Firefox --- */
* {
  scrollbar-width: thin;
  scrollbar-color: #2d3436 #101828;
}
	@font-face {
    font-family: 'Akira';
    src: url('../fonts/Akira.otf'), format('truetype');
	font-display: swap;
	}
	@font-face {
    font-family: 'PlayFair';
    src: url('../fonts/Playfair.ttf'), format('truetype');
	font-display: swap;
	}
	h1,h2,h3,h4,span{
		all:unset;
	}
	div{
		all:unset;
	}
	body{
		all: unset;
    margin: 0;
    overflow-x: hidden; /* Bloquea el horizontal (importante para el texto que sale) */
    overflow-y: auto;   /* PERMITE el vertical para que GSAP detecte el scroll */
    background: #101828;
	}

  /* Estilo para el símbolo dentro del H1 */
    #word :global(.symbol) {
        font-size: 0.3em; /* Lo hace mucho más pequeño que las letras */
        align-self: flex-start; /* Lo posiciona arriba (como superíndice) */
        margin-top: 1.5vw; /* Ajuste fino de altura */
        font-family: sans-serif; /* A veces la fuente 'Akira' no tiene el glifo ® */
    }

    /* Ajuste para que el contenedor del símbolo no sea gigante */
    #word :global(.char-container:last-child) {
        width: auto !important; /* Permite que el símbolo ocupe menos ancho */
    }

/* --- ESTILOS DEL MODAL (Correcciones solicitadas) --- */

#image-modal {
  width: 100vw;
  height: 100dvh;
  max-width: 100vw;
  max-height: 100dvh;
  margin: 0;
  padding: 0;
  border: none;
  background: transparent; /* Fondo del dialog invisible */
  overflow: hidden;
}

/* El backdrop es la capa que cubre la web detrás del modal */
#image-modal::backdrop {
  background: transparent; /* Hacemos que se vea la web de fondo */
}

.modal-content {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  /* Aplicamos un desenfoque a la web de fondo para resaltar la imagen */
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}

#modal-img {
  max-width: 100%;
  max-height: 100%;
  width: auto;
  height: auto;
  object-fit: contain; /* Ocupa el máximo pero se ve completa */
  box-shadow: 0 0 50px rgba(0,0,0,0.3); /* Sombra suave para separar de la web */
}

#close-modal {
  position: absolute;
  bottom: 30px;
  right: 30px;
  background: rgba(0, 0, 0, 0.4);
  color: white;
  border: none;
  width: 45px;
  height: 45px;
  border-radius: 50%;
  font-size: 1.5rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.3s;
  z-index: 100;
}

#close-modal:hover {
  background: rgba(255, 0, 0, 0.6); /* Feedback visual al cerrar */
}

/* Animación de entrada suave */
#image-modal[open] {
  animation: fadeIn 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}
</style>
