<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="generator" content={Astro.generator} />
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>📷</text></svg>">
		<title>Hermes | Fotografía</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
  *{
    z-index:0;
  }
	@font-face {
    font-family: 'Akira';
    src: url('../fonts/Akira.otf'), format('truetype');
	font-display: swap;
	}
	@font-face {
    font-family: 'PlayFair';
    src: url('../fonts/Playfair.ttf'), format('truetype');
	font-display: swap;
	}
	h1,h2,h3,h4,span{
		all:unset;
	}
	div{
		all:unset;
	}
	body{
		all:unset;
		margin: 0;
    overflow: hidden;
		background:#f3f1e1;

		display: grid;
		min-height: 100dvh;
		place-items: center;
		grid-template-rows: auto 1fr auto;
	}
	/* Si el foco está en el CARRUSEL, desactivamos totalmente el CONTAINER */
	div:has(.carrusel:hover) .container {
		pointer-events: none !important;; /* El container deja de existir para el mouse */
		opacity: 0;
	}

	/* Si el mouse está en cualquier parte que NO sea el container, 
	bloqueamos sus hijos para evitar disparos accidentales */
	body:not(:has(.container:hover)) .img-card-container {
		pointer-events: none !important;
	}

	header nav, .container {
		
		/* 2. La animación solo dura 1s y luego DESAPARECE */
		animation: fadeInContent 1s ease; 
		animation-delay: 2s;
		
		/* 3. IMPORTANTE: Para que no se vea antes de los 2s */
		animation-fill-mode: backwards;
	}

	@keyframes fadeInContent {
		from { opacity: 0; }
		to { opacity: 1; }
	} 

/* --- ESTILOS DEL CARRUSEL (Tus estilos originales corregidos) --- */
.carrusel {
  display: flex;
  opacity: 0;
  z-index: 0;
  flex-direction: column;
  transition: opacity 0.5s ease;
}

.carrusel:hover {
  opacity: 1;
  z-index: 100;
}

.scroll-container {
  max-width: 100vw;
  width: 100%;
  gap: 20px;
  padding: 40px;
  display: flex;
  overflow-x: auto;
  cursor: ew-resize;
  scrollbar-width: none;
  scroll-behavior: smooth;
}

.scroll-container::-webkit-scrollbar {
  display: none;
}

.img-card {
  flex: 0 0 auto;
  width: 20%;
  overflow: hidden;
}

.img-card img {
  display: block;
  width: 100%;
  height: 100%;
  cursor: pointer;
  object-fit: cover;
  transition: transform 0.3s ease;
}

.img-card:hover img {
  transform: scale(1.05);
}

/* --- ESTILOS DEL MODAL (Correcciones solicitadas) --- */

#image-modal {
  width: 100vw;
  height: 100dvh;
  max-width: 100vw;
  max-height: 100dvh;
  margin: 0;
  padding: 0;
  border: none;
  background: transparent; /* Fondo del dialog invisible */
  overflow: hidden;
}

/* El backdrop es la capa que cubre la web detrás del modal */
#image-modal::backdrop {
  background: transparent; /* Hacemos que se vea la web de fondo */
}

.modal-content {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  /* Aplicamos un desenfoque a la web de fondo para resaltar la imagen */
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}

#modal-img {
  max-width: 100%;
  max-height: 100%;
  width: auto;
  height: auto;
  object-fit: contain; /* Ocupa el máximo pero se ve completa */
  box-shadow: 0 0 50px rgba(0,0,0,0.3); /* Sombra suave para separar de la web */
}

#close-modal {
  position: absolute;
  bottom: 30px;
  right: 30px;
  background: rgba(0, 0, 0, 0.4);
  color: white;
  border: none;
  width: 45px;
  height: 45px;
  border-radius: 50%;
  font-size: 1.5rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.3s;
  z-index: 100;
}

#close-modal:hover {
  background: rgba(255, 0, 0, 0.6); /* Feedback visual al cerrar */
}

/* Animación de entrada suave */
#image-modal[open] {
  animation: fadeIn 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}

@keyframes introHermes {
  0% {
    /* Estado inicial: Centrado y Gigante */
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(1.5);
    z-index: 9999;
    margin: 0;
  }
  70% {
    /* Pausa un momento en el centro antes de moverse */
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(1.5);
  }
  100% {
    /* Estado final: Su posición en el header */
    position: relative; /* Ocupa su lugar en el flujo normal */
    top: 0;
    left: 0;
    transform: translate(0, 0) scale(1);
    z-index: 1;
  }
}

</style>
